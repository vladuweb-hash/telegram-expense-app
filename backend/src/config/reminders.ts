/**
 * Конфигурация системы напоминаний
 */
export const REMINDER_CONFIG = {
  // Время отправки напоминания (час в 24-часовом формате)
  REMINDER_HOUR: 20,
  REMINDER_MINUTE: 0,
  
  // Cron expression: каждый день в 20:00
  // Формат: секунды минуты часы день месяц день_недели
  CRON_SCHEDULE: '0 0 20 * * *',
  
  // Таймзона (по умолчанию Europe/Moscow)
  TIMEZONE: process.env.REMINDER_TIMEZONE || 'Europe/Moscow',
  
  // Сообщения напоминаний
  MESSAGES: {
    NO_EXPENSES: `📝 <b>Напоминание</b>\n\nВы ещё не записали расходы за сегодня.\n\nОткройте приложение и добавьте траты, чтобы не потерять контроль над финансами!`,
    
    NO_EXPENSES_WITH_BUTTON: `📝 <b>Напоминание</b>\n\nВы ещё не записали расходы за сегодня. Не забудьте внести траты!`,
  },
} as const;

/**
 * Настройки уведомлений по умолчанию
 */
export const DEFAULT_NOTIFICATION_SETTINGS = {
  // Напоминания включены по умолчанию
  remindersEnabled: true,
  
  // Для Premium можно отключить
  premiumCanDisable: true,
} as const;
