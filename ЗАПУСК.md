# 🚀 Как запустить проект (для новичков)

Пошаговая инструкция. Выполняй пункты по порядку.

---

## ⚡ Самый быстрый старт (только интерфейс)

Если хочешь **просто посмотреть приложение** без базы данных:

1. Открой **PowerShell** (поиск Windows → введи `PowerShell` → Enter).
2. Введи по очереди (после каждой нажми Enter):
   ```powershell
   cd C:\Users\HP\telegram-mini-app\frontend
   npm run dev
   ```
3. В конце текста появится ссылка **http://localhost:5173** — открой её в браузере.

Увидишь страницы приложения. Сохранять расходы пока не получится (для этого нужен бэкенд и база — см. ниже).

---

## Шаг 0. Что должно быть установлено

### Node.js (обязательно)
- Если не установлен: скачай с **https://nodejs.org** (версия LTS).
- Установи, перезапусти компьютер или терминал.
- Проверка: открой **PowerShell** или **cmd** и введи:
  ```text
  node -v
  ```
  Должна появиться версия, например `v20.10.0`.

### PostgreSQL (нужен для бэкенда)
- Без него **сервер не запустится**, но **интерфейс приложения** (фронтенд) можно смотреть и без него.
- Установка: см. файл **docs/POSTGRESQL.md** (там всё по шагам для Windows).
- Кратко: скачай установщик с https://www.postgresql.org/download/windows/ → установи → запомни пароль пользователя `postgres` → создай базу `telegram_mini_app` (через pgAdmin или команду из инструкции).

---

## Шаг 1. Установить зависимости

Открой терминал (PowerShell или cmd) и выполни **две** команды по очереди.

**Сначала — для сервера (бэкенд):**
```powershell
cd C:\Users\HP\telegram-mini-app\backend
npm install
```

**Потом — для приложения (фронтенд):**
```powershell
cd C:\Users\HP\telegram-mini-app\frontend
npm install
```

*В PowerShell можно писать команды через точку с запятой в одну строку, например: `cd C:\Users\HP\telegram-mini-app\backend; npm install`*

Дождись окончания (могут быть предупреждения — это нормально).

---

## Шаг 2. Настройка файлов .env

В проекте уже есть папки `backend` и `frontend`. В каждой должна быть настройка в файле **.env**.

- **backend/.env** — если его нет, скопируй из `backend/.env.example` и переименуй копию в `.env`.  
  Важно: в строке `DATABASE_URL` укажи **свой пароль** от PostgreSQL вместо `password`:
  ```text
  DATABASE_URL=postgresql://postgres:ТВОЙ_ПАРОЛЬ@localhost:5432/telegram_mini_app
  ```
- **frontend/.env** — если его нет, скопируй из `frontend/.env.example` в `.env`. Для запуска на своём компьютере можно ничего не менять.

---

## Шаг 3. База данных (если установлен PostgreSQL)

В терминале:

```powershell
cd C:\Users\HP\telegram-mini-app\backend
npm run db:migrate
```

Должно появиться что-то вроде: `Migrations completed successfully`.  
Если видишь ошибку про подключение к базе — проверь, что PostgreSQL запущен и пароль в `backend/.env` правильный (см. docs/POSTGRESQL.md).

---

## Шаг 4. Запуск

Нужно **два окна терминала** (или две вкладки).

### Терминал 1 — сервер (бэкенд)
```powershell
cd C:\Users\HP\telegram-mini-app\backend
npm run dev
```
Оставь это окно открытым. Должно появиться что-то вроде: `Server running on port 3000`.

### Терминал 2 — приложение (фронтенд)
```powershell
cd C:\Users\HP\telegram-mini-app\frontend
npm run dev
```
Оставь и это окно открытым. В конце будет ссылка, например: **http://localhost:5173**.

---

## Шаг 5. Открыть в браузере

Открой в браузере адрес: **http://localhost:5173**

Ты увидишь интерфейс приложения. Если бэкенд и база данных запущены — можно добавлять и удалять расходы, они будут сохраняться на сервере.

---

## Частые проблемы

| Проблема | Что сделать |
|----------|-------------|
| `node` не распознаётся | Установи Node.js с nodejs.org и перезапусти терминал. |
| Ошибка при `npm run db:migrate` | Установи и запусти PostgreSQL, создай базу `telegram_mini_app`, проверь пароль в `backend/.env`. |
| Бэкенд не стартует, пишет про базу | То же: проверь PostgreSQL и `DATABASE_URL` в `backend/.env`. |
| На главной «Ошибка загрузки» | Запущен ли бэкенд в первом терминале (`npm run dev` в папке `backend`)? |

---

## Кратко: только посмотреть интерфейс (без бэкенда)

Если PostgreSQL пока не ставишь:

1. Выполни только **Шаг 1** для **frontend** (и при желании для backend).
2. В одном терминале запусти только фронтенд:
   ```powershell
   cd C:\Users\HP\telegram-mini-app\frontend
   npm run dev
   ```
3. Открой в браузере **http://localhost:5173**.

Страницы откроются, но загрузка и сохранение расходов будут с ошибками, пока не запустишь бэкенд и базу.
